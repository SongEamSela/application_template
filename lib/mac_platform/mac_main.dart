import 'package:flutter/material.dart';

class MacMain extends StatelessWidget {
  const MacMain({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Welcome to Mac Application')));
  }
}
