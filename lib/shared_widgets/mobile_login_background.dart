import 'package:flutter/material.dart';

class MobileLoginBackground extends StatelessWidget {
  const MobileLoginBackground({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
