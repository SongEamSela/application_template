import 'package:flutter/material.dart';

class UnknowPlatform extends StatelessWidget {
  const UnknowPlatform({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Unknow Device Type'),
    );
  }
}
